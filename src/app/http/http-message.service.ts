import { Injectable } from '@angular/core';

@Injectable()
export class HttpMessageService {
  messages: string[] = [];

  add(message: string) {
    this.messages.push(message);
  }

  clear() {
    this.messages = [];
  }
}
